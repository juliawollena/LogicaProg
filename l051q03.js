/*Desenvolver um programa que apresente os quadrados dos números inteiros de 15 a 200.*/

let cont = 15;

while (cont <= 200){

console.log (`${cont} elevado ao quadrado é igual: ${cont * cont}`) //calculo do quadrado ou podemos usar ** 2 (ao quadrado)

    cont = cont + 1;
}